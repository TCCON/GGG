
c Global Constants
      real*4
     & spi,         !PI in single precision
     & zero,
     & unity

      real*8
     & dpi          !PI in double precision

      parameter(spi=2*acos(0.0))
      parameter(dpi=2*dacos(0.0d0))
      parameter(zero=0.0)
      parameter(unity=1.0)

