
      character
     & sptpath*32,
     & specname*57,
     & sptfile*80,
     & solarll*80

      real*4
     & tau,
     & var,
     & d_max
      parameter(tau=1.e-6,var=1.0,d_max=9.9999)

      integer*4
     & mmp
      parameter(mmp=120000)

